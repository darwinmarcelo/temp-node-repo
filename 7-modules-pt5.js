let num1 = 10
let num2 = 5

function add() {
    console.log(`Total is ${num1 + num2}`);
}

add()

// i will not declare a module export here, but i will require it to the 3-modules-pt1
// it will still be displayed because there is a executing function on the file "add()"